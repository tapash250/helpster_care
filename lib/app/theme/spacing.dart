/// Centralized spacing tokens (AGENTS.md §33, §120).
///
/// Never hardcode padding/margins. Use these tokens, e.g. `AppSpacing.md`.
class AppSpacing {
  const AppSpacing._();

  /// 4.0 logical pixels.
  static const double xs = 4;

  /// 8.0 logical pixels.
  static const double sm = 8;

  /// 16.0 logical pixels.
  static const double md = 16;

  /// 24.0 logical pixels.
  static const double lg = 24;

  /// 32.0 logical pixels.
  static const double xl = 32;

  /// 48.0 logical pixels.
  static const double xxl = 48;
}
